---
import PdfViewerReact from "./pdf/PdfViewer";
interface Props {
  stem: string;
}
const { stem } = Astro.props;
const base = import.meta.env.BASE_URL;
const b = base.endsWith("/") ? base : base + "/";
---
<PdfViewerReact client:load stem={stem} basePath={b} />
